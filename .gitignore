.DS_Store
.venv/
.superpowers/
.playwright-mcp/
# Agent design-critique scratch (session-scoped, self-contradicting snapshots) —
# same class as .superpowers/. Promote anything durable into docs/ by hand.
.impeccable/
/*.jpeg
/*.png
home-network.txt
.env
.env.*
!.env.example

# non-blog notes (AI YouTube channel) — kept local, out of the blog repo
ai-youtube-channel/

# Runtime map basemap + glyphs (binaries, served by nginx) — never commit
/map-assets/

# npm/vitest scratch created by running a tool from the repo root (the real
# trees are site/node_modules and uploader/node_modules, ignored in their own
# .gitignore files).
node_modules/
